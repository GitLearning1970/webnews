const loginScreen = document.getElementById('loginModal').style

// open login screen
loginBtn.addEventListener('click', () => loginScreen.display = 'block')
// close login screen
closeModal.addEventListener('click', () => loginScreen.display = 'none')